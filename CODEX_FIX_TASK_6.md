# Задание 6: детерминизм движка и повтор упражнений

Раунд 5 (коммит `25b4b6e`) по происхождению упражнений принят. Проверено независимо:
все 32 упражнения достигаются в PGN своего урока (30 из мейнлайна, 2 из объявленных
вариантов через `sourcePly`/`sourceLine` — Алехин drill2 и Капабланка drill4, обе линии
переигрываются и приводят ровно к заявленному FEN), повторов FEN между уроками нет,
общий пул Морфи исчез. Оба новых правила валидатора воспроизведены отрицательно
самостоятельно: подстановка чужого набора даёт «набор упражнений совпадает с уроком
byrne-fischer-1956», подстановка одной чужой позиции — «упражнение 3: FEN не достигнут
в PGN урока steinitz-bardeleben-1895».

Дальше две проблемы. Первая блокирующая.

## 1. verify:engine недетерминирован, и это скрывает реальные ошибки

`scripts/verify-with-engine.ts:56` ставит `setoption name Threads value 4`. Многопоточный
Stockfish на фиксированной глубине не воспроизводим: порядок обхода зависит от гонки
потоков за общий хеш, и оценки около порога гуляют. Пять прогонов на одном и том же
чистом дереве дали **0, 3, 0, 1, 1 ошибок**. Зелёный CI на коммите 25b4b6e — это удачный
бросок монеты, а не доказательство.

С `Threads value 1` вывод стабилен: два прогона подряд дали побайтово одинаковый список
ошибок. Время — 55 секунд на всех восьми уроках, в таймаут CI укладывается с запасом.

Требуется:

- поставить `Threads 1` (или иначе добиться воспроизводимости — но однопоточный режим
  здесь просто дешевле всего);
- в доказательство приложить два подряд прогона `npm run verify:engine` с одинаковым
  выводом (diff пустой).

Не «чинить» это понижением порогов или ретраями до зелёного.

## 2. Три настоящие ошибки движка, которые до сих пор маскировались

При `Threads 1` стабильно падает:

| Урок | Что | Факт |
|---|---|---|
| Бёрн — Фишер, 1956 | упражнение 1 (`Na4`) | отрыв 1.48 < 1.50 (attack) |
| Каспаров — Топалов, 1999 | момент ply 10, опровержение `Nd5` | линия `cxd5` не входит в допустимый топ-3 |
| Ротлеви — Рубинштейн, 1907 | момент ply 43, опровержение `Qe7` | потеря 1.45 < 1.50 (attack) |

Чинить контентом: заменить упражнение/опровержение на позицию, которая проходит порог,
либо пересмотреть заявленную линию опровержения. Пороги `themeThreshold` не трогать.

## 3. Упражнения дублируют моменты того же урока

Побочный эффект раунда 5: позиции взяли из мейнлайна, и часть из них — ровно те, которые
ученик только что решал в самом уроке как момент `find`. Та же позиция, тот же ответ.

| Урок | Повторов из 4 | Какие |
|---|---|---|
| Бёрн — Фишер | 3 | `Na4` (момент ply 21), `Be6` (ply 33), `Bxc4+` (ply 35) |
| Стейниц — фон Барделебен | 3 | `Ne6` (ply 36), `Ng5+` (ply 40), `Rxe7+` (ply 42) |
| Каспаров — Топалов | 2 | `Re7+` (ply 48), `Bf1` (ply 70) |
| Ласкер — Бауэр | 2 | `Bxh7+` (ply 28), `Bxg7` (ply 32) |
| Алехин — Йейтс | 1 | `Rxg7` (ply 72) |
| Ротлеви — Рубинштейн | 1 | `Rxc3` (ply 43) |
| Капабланка — Тартаковер | 0 | — |
| Уинтер — Капабланка | 0 | — |

Итого 12 из 32. Упражнение после урока должно проверять понимание, а не предлагать нажать
ту же кнопку второй раз через пять минут.

Требуется:

- заменить эти 12 упражнений на другие позиции той же партии (мейнлайн или объявленный
  вариант через `sourcePly`/`sourceLine`, как уже сделано) — в партиях от 49 до 119
  полуходов выбор есть;
- добавить правило в `scripts/validate-content.ts`: FEN упражнения не должен совпадать
  с позицией момента `kind: 'find'` этого же урока. Приложить отрицательное доказательство,
  как в `ROUND5_VALIDATOR_PROOF.md`;
- порог `drills.length >= 4` не понижать.

## 4. Порядок сдачи

`npm run validate:content`, два прогона `npm run verify:engine` с одинаковым выводом,
`npm run build`, коммит, CI, деплой. Отчитывайся по каждому этапу отдельным сообщением
в Telegram по ходу работы, а не одним итогом в конце.
