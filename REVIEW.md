# Ревью сборки Codex — chess-trainer

Проверено: сборка, валидатор контента, код (App.tsx, engine.ts, storage.ts), весь шахматный
контент независимо перепроверен Stockfish 18 на глубине 20 (multipv 3), живой сайт
https://yyalkov-ship-it.github.io/chess-trainer/ отвечает.

## Что сделано правильно

- `npm run build` чистая (tsc -b + vite), 295 kB JS / 95 kB gzip, PWA precache 12 файлов, 7.5 MB.
- `npm run validate:content` зелёный, и он же стоит гейтом перед деплоем в GitHub Actions.
- Stockfish 18 lite **single-threaded** — правильный выбор, на GitHub Pages многопоточный не заработал бы.
- PGN не переврали: 87 полуходов, все легальны, совпадает с реальной партией.
- Все 13 моментов указывают на **настоящие ходы партии** — ни одного выдуманного `answerSan`.
- Ключевые жертвы проверены движком и действительно лучшие: `Rxd4`, `Re7+`, `Qc3`, `Ra7`,
  `Bf1` — первый выбор Stockfish, причём `Bf1` с отрывом 12.3 пешки, `Re7+` — 4.2.
- `storage.ts` (интервальные повторения 1/3/7 дней, удаление после двух подряд верных) —
  ровно по спецификации, ошибок нет.
- Тёмная тема, размеры кнопок ≥44px, вёрстка на 375px — на месте.

## Баги, отсортированы по вреду

### 1. Оценка движка не приводится к одной стороне (критично)

`src/engine.ts`: `score cp` в UCI считается **от стороны, которая ходит**. Код кладёт его
как есть, а `humanEvaluation()` ещё и берёт `Math.abs()`, то есть окончательно теряет,
кто лучше стоит. В уроке сравниваются две оценки из разных систем координат:

```ts
const before = await engine.evaluate(chess.fen(), 700)   // ход белых → плюс = белым хорошо
const after  = await engine.evaluate(trial.fen(), 900)   // ход чёрных → плюс = ЧЁРНЫМ хорошо
```

Живой пример на моменте 11 (ply 70, надо `Bf1`). Ребёнок играет `Rb8` — зевок, после
которого белые проигрывают (−9.7). Приложение покажет:
«До хода: серьёзный перевес (3.1). После твоего хода: перевес примерно как лишняя ладья (9.7)» —
то есть похвалит за зевок. Это ломает главный обучающий механизм режима Б.

Фикс: нормализовать к POV героя — `cp = sideToMove === heroColor ? cp : -cp` — и в
`humanEvaluation` печатать знак словами («у белых лучше» / «у чёрных лучше»), не `Math.abs`.

### 2. Опровержения выдуманы (критично для содержания)

У каждого ошибочного хода поле `line` — это то, что «ответит соперник». Из 16 опровержений
одна и та же заглушка `Bb7` стоит 5 раз, `Kc8` 2 раза, `Rb8` 2 раза. Движок отвечает иначе:

| момент | ошибка | в JSON | реально |
|---|---|---|---|
| ply 34 | `Nxc5` | `Kc8` | `dxc5` — коня просто съедают (−3.2) |
| ply 54 | `Qa7` | `Bb7` | `Nxd5` |
| ply 56 | `Qxf6` | `Bb7` | `Qd1+` с матовой атакой |
| ply 14 | `Bg5` | `Rb8` | `h5` |
| ply 42 | `Qxf6` | `Bb7` | `Qxf6` |

Тексты `why` тоже мимо: про `Nxc5` написано «уводит коня от атаки», хотя настоящая причина —
конь висит. Валидатор это поймать не может, он проверяет только легальность.
Ребёнку будут показывать неправильные шахматные объяснения — ровно то, чего я и опасался.

Фикс: каждый `line` пересчитать движком (2–4 хода реального опровержения, а не один), `why`
переписать под то, что реально происходит.

### 3. Опровержения не проигрываются на доске

Спецификация требовала показать опровержение ходами. В `App.tsx` оно только печатается
текстом в панель комментария. Для десятилетнего это почти бесполезно.

### 4. Упражнения — копия самого урока

Все 8 «упражнений» — это те же 8 позиций из уже разобранной партии (ply 14, 34, 42, 46, 48,
54, 56, 70), FEN в FEN. Переноса навыка ноль: ребёнок вспомнит ход, а не идею.
Плюс два из них не имеют однозначного решения:

- упражнение 1 (`Bh6`): движок первым ставит `Nd1`, `Bh6` второй, разрыв 1 сотая пешки;
- упражнение 2 (`Na5`): `Na5` вообще не входит в тройку лучших (движок: `g4`, `Qf4`, `Qe3`), оценка 0.00.

В уроке это допустимо — мы учим замысел Каспарова. В упражнении, где ответ оценивается
«верно/неверно», ребёнка наказывают за объективно не худший ход.

Фикс: подобрать 8 отдельных позиций на те же темы (жертва за вскрытие линии, конь на d5,
охота на короля) из других партий или этюдов, с однозначным решением.

### 5. Слабые «ошибки» подаются как грубые

Проверил цену ошибок движком: `Bg5` на ply 14 теряет 0.57, `d5` — 0.72, `Nc6+` на ply 46 — 0.65.
Это ходы «чуть хуже», а не ошибки. Ребёнку скажут, что он ошибся, хотя позиция почти равна.
Для опровержений брать только ходы, теряющие от 1.5 пешки.

### 6. Приложение умеет только белых

`App.tsx`: `orientation: 'white'` захардкожен, в режиме игры `chess.turn() !== 'w'` и текст
«Ты играешь белыми». Любая партия, где герой играет чёрными (в том числе партии самого Тихона),
даст перевёрнутую доску и мёртвый экран игры. Ломает главный принцип «новая партия = один JSON».

### 7. Последний ход не подсвечивается, стрелки не рисуются

`lastMove: undefined` передаётся всегда, поэтому при перемотке партии не видно, какой ход
только что сделан. Поля `arrows` и `highlight` есть в схеме, но не рендерятся, и в JSON
их не используют ни разу — вся визуальная подсветка идей потеряна.

### 8. Мелочи

- В `engine.ts` нет таймаута и `stop`: если поиск зависнет, очередь промисов встанет навсегда
  и движок умрёт до перезагрузки страницы.
- Уровни «1200/1500/1800 Эло» — это на самом деле `Skill Level` 2/6/10. Надо `UCI_LimitStrength`
  + `UCI_Elo`, как в спецификации, иначе подписи врут.
- `registerStudyDay()` вызывается только при полном прохождении партии — серия не растёт,
  если ребёнок час решал, но не дошёл до конца.
- Момент ply 72: в JSON `Rd7+`, в партии `Rd7` — не шах, ладья d2 закрывает линию.
  chess.js это проглатывает, валидатор не ловит.
- Экран «Работа над ошибками» показывает только первую задачу по сроку: сразу после урока
  счётчик обещает 3 задачи, а внутри «Возвращайся позже».

## Приоритет исправлений

1. Знак оценки (#1) — без этого режим разбора учит неверному.
2. Опровержения: реальные линии и честные `why` (#2), проигрывание на доске (#3).
3. Новые упражнения (#4) и порог ошибки 1.5 пешки (#5).
4. Поддержка чёрных (#6), подсветка последнего хода и стрелки (#7).
5. Мелочи (#8).

Валидатор стоит усилить: сверять `answerSan` с настоящим ходом партии на этом ply,
проверять суффикс шаха, требовать, чтобы FEN упражнения не встречался в партии,
и прогонять контент движком в CI.
