# Задание Codex — раунд 4

Проверил коммиты `55beb70` и `11f9e88`. Подбор партий хороший и разбивку по типам закрывает.
Но два урока содержат не те партии, которыми они подписаны, а интерактивность в новом паке
упала почти до нуля. CI при этом зелёный — и это главная новость, важнее самих дефектов.

## 0. Почему зелёный CI ничего не доказал

`validate-content.ts` проверяет партию **саму на себя**: легальность ходов, совпадение
`answerSan` с `history[ply]`, порядок моментов. `verify-with-engine.ts` проверяет качество
ходов движком. Ни один из них не задаёт вопрос «а игралась ли вообще эта партия и теми ли
людьми». Поэтому можно взять любую партию, подписать любым именем, годом и турниром — всё
пройдёт. Это та же дыра, что была с выдуманными комбинациями на первом раунде, только
поднявшаяся на уровень выше: с ходов на происхождение.

Чинить надо и содержание, и дыру.

## 1. `alekhine-vasic-1931.json` — партия не та

В файле лежит: ферзевый гамбит, 40 ходов, `1.d4 Nf6 2.c4 e6 3.Nf3 d5 4.Nc3 Be7 5.Bg5 O-O`,
финал `39.Rh7+ Kg8 40.Rcg7#`.

Это **Алехин — Йейтс, Лондон 1922** (QGD Orthodox, D64). Алехин сам называл её своим лучшим
позиционным достижением.

Настоящая **Алехин — Васич, Баня-Лука 1931** — совсем другая партия: сеанс одновременной игры
10 января 1931, французская защита (вариант Винавера), 11 ходов, жертва ферзя на десятом,
мат Бодена.

Урок написан правильно — про сдвоенных ладей на седьмой горизонтали, и это действительно
содержание партии с Йейтсом. Поэтому чинить надо подпись, а не ходы:

- `title` → `Алехин — Йейтс, Лондон 1922`;
- `id` и имя файла → `alekhine-yates-1922`;
- проверить `intro` и тексты моментов на упоминания Васича, Баня-Луки и 1931 года.

Миниатюру с Васичем можно добавить отдельным уроком позже — она короткая и для Тихона
наглядная, но это уже другой урок, не этот.

## 2. `steinitz-bardeleben-1895.json` — продолжение выдумано

В файле после 22.Rxe7+ Kf8 идёт `23.Rf7+ Qxf7 24.Qxc8+ Rxc8 25.Rxc8+`.

В партии было `23.Rf7+ Kg8 24.Rg7+ Kh8 25.Rxh7+`, после чего фон Барделебен встал и вышел из
зала, не сдавшись и не сказав ни слова, а Стейниц показал зрителям мат в десять ходов:
`25...Kg8 26.Rg7+ Kh8 27.Qh4+ Kxg7 28.Qh7+ Kf8 29.Qh8+ Ke7 30.Qg7+ Ke8 31.Qg8+ Ke7 32.Qf7+
Kd8 33.Qf8+ Qe8 34.Nf7+ Kd7 35.Qd6#`.

`23...Qxf7` — это побочный вариант, который Стейниц как раз и рассчитывал. Подставив его в
партию, урок вырезал именно то, чем эта партия знаменита: ладью, которую нельзя взять четыре
хода подряд.

Верни настоящее продолжение. Мат в десять хорошо ложится в объяснение к моменту — но как
вариант, а не как основная линия партии.

## 3. Корневой фикс: происхождение партии проверяется механически

Сделай так, чтобы подмена партии падала в CI, а не находилась глазами.

- Каждая учебная партия получает поле `source`:
  `{ file, white, black, event, year, eco, url }`, где `file` — имя PGN в
  `src/content/sources/`, `url` — ссылка на партию в открытой базе (chessgames.com,
  365chess.com или lichess).
- В PGN-источнике у партии обязателен полный набор тегов: `White`, `Black`, `Event`, `Site`,
  `Date`, `Result`, `ECO`.
- `validate-content.ts` дополнительно проверяет:
  - партия с такими `White`/`Black`/`Date` есть в указанном PGN-файле;
  - ходы урока **посимвольно** совпадают с ходами источника, без сокращений и без «дописанных»
    вариантов;
  - `result` в JSON совпадает с тегом `Result`;
  - год в `title` совпадает с годом в `Date`.

Это не доказывает историческую правду, но сводит её к одному файлу, который можно один раз
вычитать и потом не трогать. Сейчас правда размазана по восьми JSON и не проверяется нигде.

## 4. Интерактивность новых уроков

Сейчас в каждом из шести новых уроков **один** ход для поиска и **ноль** упражнений.
Для сравнения: Фишер — 7 и 6, Каспаров — 8 и 8.

Коммит назывался «add interactive tasks», но по факту добавил по одному заданию на партию.
В таком виде шесть новых уроков — это перелистывание с комментариями. Ребёнок смотрит, а не
решает, а весь смысл тренажёра в том, чтобы он искал ходы сам.

Доведи каждый новый урок до уровня старых: **минимум 5 моментов `find`** и **минимум
4 упражнения**. И закрепи это правилом в `validate-content.ts`, иначе следующий пак опять
проедет.

Важно: минимум пять — это не «раскидать find по случайным ходам». Момент должен быть местом,
где есть что найти: жертва, перевод фигуры, единственная защита, начало плана.

## 5. Пороги движка под позиционные партии и эндшпиль

Порог 1.50 пешки откалиброван на атакующих партиях. В позиционной игре и эндшпиле сильнейший
ход часто выигрывает не материал, а качество позиции, и отрыв там честно меньше — это
особенность жанра, а не слабый контент.

Не занижай порог глобально. Введи поле `theme` (`attack` | `positional` | `endgame`) и пороги
по темам, например 1.50 / 0.80 / 0.60. Для эндшпиля добавь более осмысленный критерий вместо
голых пешек: ход считается единственным, если лучший держит оценку выше +2.0, а второй по
силе падает ниже +0.5 — то есть второй ход меняет класс результата, а не просто «чуть хуже».

Поле `theme` заодно закрывает то, что просил Yury: типы партий сейчас в интерфейсе не видны,
на главной просто список из восьми уроков. Сгруппируй список по темам — атака на короля,
позиционная игра, эндшпиль.

## 6. Мелочь: опровержение в `rotlewi-rubinstein-1907.json`

Момент `ply 43` (22...Rxc3), ошибка `Qe7`: указано наказание `Rae1`. Stockfish 18 на глубине 20
не ставит его в тройку лучших — там `f5`, `Bg2`, `Bf3`. Потеря позиции настоящая (3.74), но
предложенный вариант неточен. Замени первым ходом из тройки движка.

## Критерии приёмки

1. Урок с ходами Алехина против Йейтса подписан Йейтсом, Лондоном и 1922 годом; в текстах
   не осталось Васича и Баня-Луки.
2. Партия Стейница идёт по настоящей партии до 25.Rxh7+; мат в десять — в объяснении.
3. `validate-content.ts` сверяет ходы урока с PGN-источником и падает при расхождении.
   Покажи, что падает: временно измени один ход и приложи вывод.
4. В каждом уроке минимум 5 моментов `find` и 4 упражнения, это проверяется валидатором.
5. `theme` есть у всех восьми партий, пороги движка зависят от темы, главная сгруппирована.
6. `validate:content`, `verify:engine`, `build`, CI и деплой зелёные; живой бандл совпадает
   с локальным.
7. В журнале проекта записано, откуда взяты PGN всех восьми партий.
