# Задание Codex: второй раунд доработки chess-trainer

Прошлый заход принят: контент действительно пересчитан движком, независимая перепроверка
на других настройках Stockfish это подтвердила. Разбор того, что осталось — в `REVIEW-2.md`
в корне репозитория, прочитай его первым делом.

Стек не менять, с нуля не переписывать. Правила прежние: **любое утверждение о шахматах,
попадающее в JSON, должно быть посчитано движком, а не придумано.** Позиции для упражнений
брать только проигрыванием PGN из `src/content/sources/`, не по памяти.

## 1. Стрелки в уроке — и защита от спойлера

Сейчас механизм `arrows`/`highlight` работает, но в `kasparov-topalov-1999.json` не проставлено
ни одной стрелки. Заполнить.

Сначала важное: в `src/App.tsx` доска получает `moment` всегда, поэтому стрелки нарисуются
**до того, как ребёнок решил задание**. Если положить туда стрелку правильного хода — задание
испорчено. Поэтому:

- в моментах `kind: "explain"` стрелки показываются сразу;
- в моментах `kind: "find"` стрелки показываются **только после того, как момент решён или
  ответ раскрыт**. Передавать `moment` на доску с учётом этого;
- на подсказки это не распространяется: подсказка остаётся текстом.

Проставить стрелки минимум в шести моментах. Для решённых `find` — направление хода-ответа и
поле, за которое идёт борьба; для `explain` — идея, о которой говорит комментарий. Цвета:
`green` для замысла героя, `red` для угрозы соперника.

Валидатор должен проверять, что каждое поле в `arrows`/`highlight` — реальное имя поля (a1–h8),
и что стрелок минимум в шести моментах партии.

## 2. Настоящая партия за чёрных вместо «дурацкого мата»

`fools-mate.json` удалить. Вместо неё — Бёрн — Фишер, Нью-Йорк 1956, «партия века»: Фишер играет
чёрными, ему тут 13 лет, и это лучшее, что можно дать десятилетнему.

PGN проверен мной python-chess: 82 полухода, все легальны, партия кончается матом, ошибок разбора нет.
Вставить как есть, **не «исправлять» по памяти**:

```
1.Nf3 Nf6 2.c4 g6 3.Nc3 Bg7 4.d4 O-O 5.Bf4 d5 6.Qb3 dxc4 7.Qxc4 c6 8.e4 Nbd7
9.Rd1 Nb6 10.Qc5 Bg4 11.Bg5 Na4 12.Qa3 Nxc3 13.bxc3 Nxe4 14.Bxe7 Qb6 15.Bc4 Nxc3
16.Bc5 Rfe8+ 17.Kf1 Be6 18.Bxb6 Bxc4+ 19.Kg1 Ne2+ 20.Kf1 Nxd4+ 21.Kg1 Ne2+ 22.Kf1 Nc3+
23.Kg1 axb6 24.Qb4 Ra4 25.Qxb6 Nxd1 26.h3 Rxa2 27.Kh2 Nxf2 28.Re1 Rxe1 29.Qd8+ Bf8
30.Nxe1 Bd5 31.Nf3 Ne4 32.Qb8 b5 33.h4 h5 34.Ne5 Kg7 35.Kg1 Bc5+ 36.Kf1 Ng3+ 37.Ke1 Bb4+
38.Kd1 Bb3+ 39.Kc1 Ne2+ 40.Kb1 Nc3+ 41.Kc1 Rc2# 0-1
```

`heroColor: "b"`. Все моменты `find` обязаны приходиться на ходы чёрных — это нечётные `ply`,
валидатор такое уже проверяет.

Опорные полуходы (индексы проверены):

| ход | ply | о чём |
|---|---|---|
| 11...Na4 | 21 | знаменитая жертва, конь идёт под бой ради инициативы |
| 13...Nxe4 | 25 | вскрытие центра, пока белый король застрял |
| 16...Rfe8+ | 31 | ладья входит в игру с шахом |
| 17...Be6 | 33 | тихий ход и жертва ферзя — главный момент партии |
| 18...Bxc4+ | 35 | начало «мельницы» |
| 24...Ra4 | 47 | ладья переходит на активную позицию |
| 26...Rxa2 | 51 | реализация материала |
| 41...Rc2# | 81 | финальный мат |

Сделать 10–12 моментов: не меньше семи `find` (взять из таблицы) и остальные `explain`.
Требования к содержанию те же, что и в прошлый раз: минимум две подсказки и два опровержения на
каждый `find`, опровержение теряет не меньше 1.5 пешки, вариант в `line` — 2–4 хода из главного
варианта движка, `why` описывает то, что реально происходит.

Плюс 6 упражнений для этой партии — позиции, где ходят **чёрные**. Брать из партий, добавленных
в `src/content/sources/`, по тем же правилам: ответ — первый выбор движка, отрыв не меньше 1.5,
позиция не встречается ни в одной учебной партии.

## 3. Сила движка выставляется дважды

В `src/engine.ts` при выборе уровня одновременно посылаются `UCI_LimitStrength` + `UCI_Elo`
**и** `Skill Level` 2/6/10. Ограничения складываются, компьютер играет заметно слабее подписи
«1200/1500/1800 Эло».

Посылать `Skill Level` только тогда, когда `UCI_LimitStrength` не поддерживается движком
(проверить по списку опций, который Stockfish печатает после `uci`). Если поддерживается —
`Skill Level` держать на 20.

## 4. Порядок и сложность упражнений

- Упражнения 4 и 5 идут в обратном порядке: четвёртое — мат `Rd8#` (17-й ход Оперной партии),
  пятое — жертва ферзя `Qb8+` (16-й ход), которая к этому мату и ведёт. Решив четвёртое, ребёнок
  уже знает ответ на пятое. Поменять местами и вообще упорядочить упражнения по ходу партии-источника.
- Три упражнения из восьми — мат в один ход (`Rd8#`, `Bxe7#`, `Be7#`). Для первого юношеского
  разряда это разминка. Оставить одно, два заменить позициями, где решение — комбинация в 2–3 хода
  (ответом по-прежнему считается первый ход, но в `explanation` показать весь вариант).

## 5. Сделать проверку движком устойчивее

- Гонять Stockfish в `scripts/verify-with-engine.ts` на более сильных настройках: `Threads 4`,
  `Hash 256`. На нынешних (1 поток, 32 МБ) отрыв в упражнении 2 (`Rxd7`) насчитался 1.81, а на
  сильных — 1.06, то есть порог держался только благодаря слабым настройкам.
- Смягчить проверку первого хода варианта: сейчас требуется точное совпадение с выбором движка,
  а в выигранных позициях выигрывающих ходов несколько, и проверка ломается на ровном месте
  (`Rhxe8` против `Qxf4`, `Rhxe8` против `Rdxe8`). Считать вариант верным, если его первый ход
  **входит в тройку лучших и отстаёт от лучшего не больше чем на 0.5 пешки**. Если не входит —
  это по-прежнему ошибка.

## 6. Косметика

- На главной подпись «{n} партия» при двух и более партиях. Сделать правильные окончания:
  партия / партии / партий.
- Экран пустых упражнений говорит «Ты повторил главные идеи партии» после «0 из 0». Показывать
  этот текст только если упражнения действительно были.

## Критерии приёмки

1. `npm run validate:content`, `npm run verify:engine`, `npm run build` проходят, все три в CI.
2. `fools-mate.json` удалён, вместо него Бёрн — Фишер с `heroColor: "b"`, 10–12 моментов
   (≥7 типа `find`) и 6 упражнений с ходом чёрных.
3. `verify:engine` печатает по каждому опровержению обеих партий цену ошибки, и она везде ≥1.5
   пешки на настройках Threads 4 / Hash 256.
4. Каждое упражнение обеих партий: ответ — первый выбор движка, отрыв ≥1.5 на тех же настройках,
   FEN не встречается ни в одной учебной партии.
5. Стрелки стоят минимум в шести моментах партии Каспарова и минимум в шести моментах партии
   Фишера, и в моментах `find` они **не видны до решения** — проверь руками, что задание не
   спойлерится.
6. При выборе уровня в лог движка уходит либо `UCI_Elo`, либо `Skill Level`, но не оба сразу.
7. Упражнения идут в порядке партии-источника; мат в один ход остался ровно один.
8. Обе партии проходятся целиком: доска повёрнута к своей стороне, режим игры работает и за белых,
   и за чёрных, работа над ошибками наполняется в обеих.

В конце напиши коротко: что поправил, какие моменты и упражнения добавил для партии Фишера,
и приложи полный вывод `npm run verify:engine`.
